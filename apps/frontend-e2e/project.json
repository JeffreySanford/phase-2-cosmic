{
  "name": "frontend-e2e",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/frontend-e2e/src",
  "tags": [],
  "implicitDependencies": ["frontend"],
  "// targets": "to see all targets run: nx show project frontend-e2e --web",
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["apps/frontend-e2e/**/*.ts"]
      }
    },
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./",
        "command": "cross-env CYPRESS_webServerCommand=\"pnpm exec concurrently --kill-others-on-fail --success first \\\"cross-env FRONTEND_PORT=4000 DEV_SERVER_ORIGIN=http://127.0.0.1:4200 pnpm run serve:ssr\\\" \\\"pnpm exec nx run frontend:serve --host=127.0.0.1\\\"\" CYPRESS_BASE_URL=\"http://127.0.0.1:4200\" node scripts/run-cypress.js run --project apps/frontend-e2e"
      },
      "cache": true,
      "inputs": ["default", "^default"],
      "outputs": [
        "{workspaceRoot}/dist/cypress/apps/frontend-e2e/videos",
        "{workspaceRoot}/dist/cypress/apps/frontend-e2e/screenshots"
      ],
      "parallelism": false,
      "metadata": {
        "technologies": ["cypress"],
        "description": "Runs Cypress Tests"
      }
    },
    "e2e-ci": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./",
        "command": "cross-env CYPRESS_webServerCommand=\"pnpm exec concurrently --kill-others-on-fail --success first \\\"cross-env FRONTEND_PORT=4000 DEV_SERVER_ORIGIN=http://127.0.0.1:4200 pnpm run serve:ssr\\\" \\\"pnpm exec nx run frontend:serve --host=127.0.0.1\\\"\" CYPRESS_BASE_URL=\"http://127.0.0.1:4200\" node scripts/run-cypress.js run --project apps/frontend-e2e"
      },
      "cache": true,
      "inputs": ["default", "^default"],
      "outputs": [
        "{workspaceRoot}/dist/cypress/apps/frontend-e2e/videos",
        "{workspaceRoot}/dist/cypress/apps/frontend-e2e/screenshots"
      ],
      "parallelism": false,
      "metadata": {
        "technologies": ["cypress"],
        "description": "Runs Cypress Tests in CI mode"
      }
    },
    "playwright-ci": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./",
        "command": "pnpm run playwright:install && pnpm run e2e:playwright"
      },
      "cache": true,
      "inputs": ["default", "^default"],
      "outputs": ["{workspaceRoot}/playwright-report"],
      "parallelism": false,
      "metadata": {
        "technologies": ["playwright"],
        "description": "Runs Playwright E2E tests in CI mode"
      }
    }
  }
}
