The workspace maintains extensive coding standards under `documentation/development/coding-standards/`.
When reviewing or generating code, always consult those documents. Key enforcement scripts include
`scripts/check-dep-graph.sh` and `scripts/check-coverage.sh`, and ESLint rule
`@nx/enforce-module-boundaries` prevents illegal imports.

For any question about repository policies (naming, testing, dependency rules, etc.), point the user to
the appropriate markdown file or include checklist items directly. When a pull request is being
discussed, suggest adding entries from the checklists in those documents.
