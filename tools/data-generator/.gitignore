# Build output only. These patterns are anchored with a leading slash so they
# match the compiled binary at the module root and NOT the cmd/data-generator
# source directory, which an unanchored pattern would silently swallow.
/data-generator
/data-generator.exe
