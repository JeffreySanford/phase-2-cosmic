# Pulsar broker configuration
# clusterName must be a literal value since the broker does not
# perform environment variable interpolation during startup. The
# value is also exposed in .env and .env.sample for convenience.
clusterName=pulsar-cluster
# ...other broker settings can be added here...
metadataStoreUrl=zk:zookeeper:2181
configurationStoreUrl=zookeeper:2181
advertisedAddress=pulsar
bookkeeperMetadataServiceUri=zk+hierarchical://zookeeper:2181/ledgers
managedLedgerDefaultEnsembleSize=1
managedLedgerDefaultWriteQuorum=1
managedLedgerDefaultAckQuorum=1
