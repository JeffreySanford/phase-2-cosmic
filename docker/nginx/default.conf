server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  access_log /var/log/nginx/static-access.log cosmic_main;
  error_log /var/log/nginx/static-error.log warn;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
